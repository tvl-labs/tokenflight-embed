# TokenFlight ethers Example

## Run locally

```bash
pnpm install
pnpm --filter @tokenflight/swap build
pnpm --filter @tokenflight/adapter-ethers build
pnpm --filter @tokenflight/example-ethers dev
```

This example requires an injected EVM wallet (for example MetaMask).
