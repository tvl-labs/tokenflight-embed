# TokenFlight wagmi Example

## Run locally

```bash
pnpm install
pnpm --filter @tokenflight/swap build
pnpm --filter @tokenflight/adapter-wagmi build
pnpm --filter @tokenflight/example-wagmi dev
```

This example uses an injected wallet connector and does not require API keys.
