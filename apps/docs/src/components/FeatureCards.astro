---
/**
 * Feature cards section — highlights key selling points of the SDK.
 * Rendered between Hero and Playground on the homepage.
 */
---

<section class="tf-features not-content">
  <div class="tf-features-grid">
    <div class="tf-feature-card">
      <div class="tf-feature-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"/><polyline points="2 17 12 22 22 17"/><polyline points="2 12 12 17 22 12"/></svg>
      </div>
      <h3 class="tf-feature-title">Any Framework</h3>
      <p class="tf-feature-desc">Web Components work everywhere — React, Vue, Svelte, or plain HTML. One integration for all.</p>
    </div>
    <div class="tf-feature-card">
      <div class="tf-feature-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
      </div>
      <h3 class="tf-feature-title">Cross-Chain</h3>
      <p class="tf-feature-desc">15+ chains supported including Ethereum, Base, Arbitrum, Solana, and more.</p>
    </div>
    <div class="tf-feature-card">
      <div class="tf-feature-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
      </div>
      <h3 class="tf-feature-title">&lt; 70 KB</h3>
      <p class="tf-feature-desc">Lightweight gzipped bundle with zero heavy dependencies. Loads in milliseconds.</p>
    </div>
    <div class="tf-feature-card">
      <div class="tf-feature-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z"/></svg>
      </div>
      <h3 class="tf-feature-title">Customizable</h3>
      <p class="tf-feature-desc">Themes, accent colors, i18n in 5 languages, and white-label branding.</p>
    </div>
  </div>
</section>

<style>
  .tf-features {
    max-width: 820px;
    margin: 0 auto;
    padding: 0 1.5rem var(--pg-section-gap-sm, 3.5rem);
  }

  .tf-features-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
  }

  .tf-feature-card {
    background: var(--pg-surface-raised, #1E2230);
    border: 1px solid var(--pg-border, #252A38);
    border-radius: 14px;
    padding: 1.25rem;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .tf-feature-card:hover {
    border-color: var(--sl-color-accent, #6C5CE7);
    transform: translateY(-2px);
  }

  .tf-feature-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    background: rgba(108, 92, 231, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--sl-color-accent, #6C5CE7);
    margin-bottom: 0.875rem;
  }

  .tf-feature-title {
    font-size: 0.9375rem;
    font-weight: 650;
    color: var(--pg-text, #EEF0F6);
    margin: 0 0 0.375rem;
    letter-spacing: -0.01em;
  }

  .tf-feature-desc {
    font-size: 0.8125rem;
    line-height: 1.55;
    color: var(--pg-text-secondary, #8B93A5);
    margin: 0;
  }

  @media (max-width: 767px) {
    .tf-features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 420px) {
    .tf-features {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .tf-features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
