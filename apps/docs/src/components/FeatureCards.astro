---
/**
 * Feature sections — each feature as a standalone full-width section.
 * Alternating layout: odd = icon left / text right, even = text left / icon right.
 */
---

<div class="tf-features not-content">

  <!-- 1. Any Framework -->
  <section class="tf-feat">
    <div class="tf-feat-visual">
      <div class="tf-feat-icon tf-feat-icon--fw">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
          <defs><linearGradient id="ic-fw-lg" x1="2" y1="2" x2="22" y2="22" gradientUnits="userSpaceOnUse">
            <stop stop-color="#6C5CE7"/><stop offset="1" stop-color="#38BDF8"/>
          </linearGradient></defs>
          <polygon points="12 2 2 7 12 12 22 7 12 2" fill="url(#ic-fw-lg)" opacity="0.9"/>
          <polyline points="2 17 12 22 22 17" stroke="url(#ic-fw-lg)" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
          <polyline points="2 12 12 17 22 12" stroke="url(#ic-fw-lg)" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
    </div>
    <div class="tf-feat-content">
      <span class="tf-feat-badge">Universal</span>
      <h3 class="tf-feat-title">Any Framework</h3>
      <p class="tf-feat-desc">Web Components work everywhere — React, Vue, Svelte, Angular, or plain HTML. One integration for all your projects.</p>
      <ul class="tf-feat-list">
        <li>Standard Web Components (Custom Elements v1)</li>
        <li>Shadow DOM encapsulation — no style conflicts</li>
        <li>Works with SSR and static sites</li>
      </ul>
    </div>
  </section>

  <!-- 2. Cross-Chain -->
  <section class="tf-feat tf-feat--reverse">
    <div class="tf-feat-visual">
      <div class="tf-feat-icon tf-feat-icon--cc">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
          <defs><linearGradient id="ic-cc-lg" x1="2" y1="2" x2="22" y2="22" gradientUnits="userSpaceOnUse">
            <stop stop-color="#6C5CE7"/><stop offset="1" stop-color="#14B8A6"/>
          </linearGradient></defs>
          <circle cx="12" cy="12" r="10" stroke="url(#ic-cc-lg)" stroke-width="2"/>
          <line x1="2" y1="12" x2="22" y2="12" stroke="url(#ic-cc-lg)" stroke-width="2"/>
          <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" stroke="url(#ic-cc-lg)" stroke-width="2" fill="none"/>
        </svg>
      </div>
    </div>
    <div class="tf-feat-content">
      <span class="tf-feat-badge">Multi-Chain</span>
      <h3 class="tf-feat-title">Cross-Chain</h3>
      <p class="tf-feat-desc">15+ chains supported including Ethereum, Base, Arbitrum, Solana, and more. Users swap from any chain to any token.</p>
      <ul class="tf-feat-list">
        <li>EVM + Solana support out of the box</li>
        <li>Best route auto-selection across bridges</li>
        <li>Real-time quotes with streaming updates</li>
      </ul>
    </div>
  </section>

  <!-- 3. Lightweight -->
  <section class="tf-feat">
    <div class="tf-feat-visual">
      <div class="tf-feat-icon tf-feat-icon--lt">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
          <defs><linearGradient id="ic-lt-lg" x1="3" y1="2" x2="21" y2="22" gradientUnits="userSpaceOnUse">
            <stop stop-color="#6C5CE7"/><stop offset="1" stop-color="#F59E0B"/>
          </linearGradient></defs>
          <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" fill="url(#ic-lt-lg)" opacity="0.9"/>
        </svg>
      </div>
    </div>
    <div class="tf-feat-content">
      <span class="tf-feat-badge">Performance</span>
      <h3 class="tf-feat-title">&lt; 70 KB</h3>
      <p class="tf-feat-desc">Lightweight gzipped bundle with zero heavy dependencies. Loads in milliseconds, no impact on your Core Web Vitals.</p>
      <ul class="tf-feat-list">
        <li>Tree-shakeable ESM + UMD outputs</li>
        <li>Zero runtime dependencies</li>
        <li>Lazy-loaded assets and icons</li>
      </ul>
    </div>
  </section>

  <!-- 4. Customizable -->
  <section class="tf-feat tf-feat--reverse">
    <div class="tf-feat-visual">
      <div class="tf-feat-icon tf-feat-icon--cust">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
          <defs><linearGradient id="ic-cust-lg" x1="6" y1="2" x2="18" y2="22" gradientUnits="userSpaceOnUse">
            <stop stop-color="#6C5CE7"/><stop offset="1" stop-color="#EC4899"/>
          </linearGradient></defs>
          <path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z" fill="url(#ic-cust-lg)" opacity="0.9"/>
        </svg>
      </div>
    </div>
    <div class="tf-feat-content">
      <span class="tf-feat-badge">Branding</span>
      <h3 class="tf-feat-title">Customizable</h3>
      <p class="tf-feat-desc">Themes, accent colors, i18n in 5 languages, and white-label branding. Make the widget look like it's yours.</p>
      <ul class="tf-feat-list">
        <li>Light & dark theme with auto-detection</li>
        <li>Custom accent colors and title branding</li>
        <li>EN, 中文, 繁體, 日本語, 한국어</li>
      </ul>
    </div>
  </section>

</div>

<style>
  .tf-features {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* ── Each Feature Section ── */
  .tf-feat {
    display: grid;
    grid-template-columns: 200px 1fr;
    align-items: center;
    gap: 2.5rem;
    padding: 2.5rem;
    border-radius: 20px;
    border: 1px solid var(--pg-border, #252A38);
    background: var(--pg-surface-raised, #1E2230);
    position: relative;
    transition: border-color 0.25s ease, transform 0.25s ease, box-shadow 0.25s ease;
  }

  .tf-feat::before {
    content: '';
    position: absolute;
    inset: -1px;
    border-radius: 21px;
    background: var(--tf-grad-card-border, linear-gradient(135deg, rgba(108,92,231,0.4), rgba(56,189,248,0.2), rgba(236,72,153,0.3)));
    opacity: 0;
    transition: opacity 0.25s ease;
    z-index: -1;
  }

  .tf-feat:hover {
    border-color: transparent;
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(108, 92, 231, 0.08);
  }

  .tf-feat:hover::before {
    opacity: 1;
  }

  /* Reverse layout for alternating sections */
  .tf-feat--reverse {
    grid-template-columns: 1fr 200px;
  }

  .tf-feat--reverse .tf-feat-visual {
    order: 2;
  }

  .tf-feat--reverse .tf-feat-content {
    order: 1;
  }

  /* ── Visual / Icon ── */
  .tf-feat-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tf-feat-icon {
    width: 100px;
    height: 100px;
    border-radius: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .tf-feat:hover .tf-feat-icon {
    transform: scale(1.05);
  }

  .tf-feat-icon--fw {
    background: linear-gradient(135deg, rgba(108,92,231,0.12) 0%, rgba(56,189,248,0.08) 100%);
    box-shadow: 0 0 0 1px rgba(56,189,248,0.1);
  }

  .tf-feat:hover .tf-feat-icon--fw {
    box-shadow: 0 8px 24px rgba(56,189,248,0.12);
  }

  .tf-feat-icon--cc {
    background: linear-gradient(135deg, rgba(108,92,231,0.12) 0%, rgba(20,184,166,0.08) 100%);
    box-shadow: 0 0 0 1px rgba(20,184,166,0.1);
  }

  .tf-feat:hover .tf-feat-icon--cc {
    box-shadow: 0 8px 24px rgba(20,184,166,0.12);
  }

  .tf-feat-icon--lt {
    background: linear-gradient(135deg, rgba(108,92,231,0.12) 0%, rgba(245,158,11,0.08) 100%);
    box-shadow: 0 0 0 1px rgba(245,158,11,0.1);
  }

  .tf-feat:hover .tf-feat-icon--lt {
    box-shadow: 0 8px 24px rgba(245,158,11,0.12);
  }

  .tf-feat-icon--cust {
    background: linear-gradient(135deg, rgba(108,92,231,0.12) 0%, rgba(236,72,153,0.08) 100%);
    box-shadow: 0 0 0 1px rgba(236,72,153,0.1);
  }

  .tf-feat:hover .tf-feat-icon--cust {
    box-shadow: 0 8px 24px rgba(236,72,153,0.12);
  }

  /* ── Content ── */
  .tf-feat-content {
    min-width: 0;
  }

  .tf-feat-badge {
    display: inline-block;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--sl-color-accent, #6C5CE7);
    background: rgba(108, 92, 231, 0.1);
    padding: 3px 10px;
    border-radius: 999px;
    margin-bottom: 0.75rem;
  }

  .tf-feat-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--pg-text, #EEF0F6);
    margin: 0 0 0.5rem;
    letter-spacing: -0.02em;
  }

  .tf-feat-desc {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--pg-text-secondary, #8B93A5);
    margin: 0 0 1rem;
  }

  .tf-feat-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .tf-feat-list li {
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--pg-text-secondary, #8B93A5);
    padding-left: 1.25rem;
    position: relative;
  }

  .tf-feat-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.5em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--sl-color-accent, #6C5CE7);
    opacity: 0.5;
  }

  /* ── Responsive ── */
  @media (max-width: 767px) {
    .tf-features {
      gap: 1.5rem;
    }

    .tf-feat {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      padding: 1.5rem;
      text-align: center;
    }

    .tf-feat--reverse {
      grid-template-columns: 1fr;
    }

    .tf-feat--reverse .tf-feat-visual {
      order: 0;
    }

    .tf-feat--reverse .tf-feat-content {
      order: 0;
    }

    .tf-feat-icon {
      width: 80px;
      height: 80px;
      border-radius: 20px;
    }

    .tf-feat-icon svg {
      width: 40px;
      height: 40px;
    }

    .tf-feat-title {
      font-size: 1.25rem;
    }

    .tf-feat-list {
      text-align: left;
    }
  }
</style>
